#include "compiler.h"

#include <stdlib.h>
#include <string.h>


bool
cp_compile(const char* code, uint8_t** buf, size_t sz)
{
    (void) code;
    (void) buf;
    (void) sz;

    return true;
}


// vim: ts=4:sw=4:sts=4:expandtab:foldmethod=marker
